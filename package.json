{
  "name": "seismic-ambient",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && vercel --prod"
  },
  "dependencies": {
    "tone": "^14.8.49"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
